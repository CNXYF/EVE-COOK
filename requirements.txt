# ============================================================
# EVE-COOK 依赖清单
# ------------------------------------------------------------
# 安装方式（在已激活的虚拟环境中执行）：
#   pip install -r requirements.txt
#
# 说明：
#   - Python 版本限定 3.10 / 3.11（避免 3.12 兼容性问题）
#   - pywin32 仅限 Windows 系统
# ============================================================

# UI 框架（强制约束：不可替换为 PySide6）
PyQt5>=5.15.9
PyQt5-Qt5>=5.15.2

# Windows API（窗口枚举 / 截图）⚠️ 仅限 Windows
pywin32>=306

# 图像处理（窗口截图后处理）
Pillow>=10.0.0

# 文件监控（Observer 模式，禁止轮询）
watchdog>=3.0.0

# 音频 / TTS 语音播报
pyttsx3>=2.90
playsound>=1.3.0

# 网络请求（翻译服务，必须设置 timeout>=5s 且在子线程执行）
requests>=2.31.0

# 图算法（跳数计算）
networkx>=3.1

# 配置 / SDE 数据解析
PyYAML>=6.0
