"""
============================================================
模块：utils 包初始化文件
------------------------------------------------------------
功能说明：
    utils 是"工具层"，存放与业务无关的通用工具，
    例如统一日志（logger.py）和常量定义（constants.py）。
    本文件为空，作用是告诉 Python："utils 是一个包（package）"，
    这样才能用 `from utils.logger import get_logger` 的方式导入。
============================================================
"""
